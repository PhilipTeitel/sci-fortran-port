<!-- Dependency ledger contract:
- Every dependency row must name support status, target-stack path, and substitution decision.
- Use `blocked` when there is no credible replacement, wrapper, or reimplementation route.
- If a section has no content yet, write `None yet.`.
-->

# Dependency Ledger

**Legacy repo:** `/Users/philipteitel/code/ADD-migrations/sci-fortran-legacy` (read-only; explicit user override of the configured `../scifortran-legacy`)
**Source stack:** Fortran 90/95-style modules with Fortran 2003-era command intrinsics; exact dialect, compiler, and production versions unresolved
**Target stack:** C# / .NET 8 / ASP.NET Core
**Date:** `2026-08-09`

---

## 1. Dependency summary

| Category | Count | Unsupported | Blocked | Notes |
|----------|-------|-------------|---------|-------|
| Runtime / compiler | 3 | 1 | 1 | `ifort` is discontinued; the production compiler and external Fortran ABI consumers are unknown. |
| Build / compiler tooling | 4 | 0 | 1 | Make, archive, and revision-stamping paths are reconstructable; OpenMP intent is unresolved. |
| Shell / deployment | 4 | 1 | 2 | Setup is host-coupled and currently broken; MPICH2 1.4.1 is obsolete. |
| Native math / FFT | 7 | 1 | 7 | BLAS/LAPACK provider and production FFT backend/version are unresolved; FFTPACK callees are absent. |
| Vendored code | 7 | 3 | 3 | Bundled code has mixed or missing provenance; the `ZEROS` module is absent. |
| Plotting / tooling | 3 | 2 | 2 | Gnuplot can credibly be wrapped; `libmatheval` is unmaintained and `DLPLOT` is missing. |
| File / protocol integration | 4 | 0 | 1 | File operations are portable, but CLI/API boundary and per-surface text contracts require decisions. |
| **Total** | **32** | **8** | **17** | A blocked route is not approved for implementation or skeleton planning. |

No database, network API, message bus, scheduler, web framework, container, or orchestration dependency was found in the inspected legacy checkout. `E3 code-derived` — `docs/modernization/legacy-map.md:20-21,121,140`.

**Tensions / conflicts (affected routes remain blocked):**

- `master`/`5e4e6a3` and branch-only `initial-build`/`9b19827` disagree on build reconstruction, OpenBLAS use, FFT backend, facade composition, and fidelity assets. The authoritative baseline is unresolved. `E1 verified / E3 code-derived` — `docs/modernization/intent-ledger.md:47,60,81,85-87`.
- Current `FFTGF.f90` selects MKL, while FFTW3, Numerical Recipes, and FFTPACK implementations also exist; setup script filenames do not match the tracked files. `E3 code-derived / E5 unknown` — `src/FFTGF.f90:1-8`; `bin/setup_sf.sh:193-202`; `docs/modernization/legacy-map.md:201-203`.
- `SCIFOR` imports `ZEROS`, but the source/build target is absent; history says it was renamed to `OPTIMIZE`, while only the branch changes the facade. `E3 code-derived / E5 unknown` — `src/SCIFOR.f90:1-17`; `docs/modernization/intent-ledger.md:58,84`.
- Text formatting and complex-column order differ by surface, so there is no safe global text-codec substitution. `E3 code-derived` — `docs/modernization/intent-ledger.md:24-25,87,89`.
- The workflow profile has a legacy-path mismatch and malformed source-stack compiler structure; this ledger uses the explicit user path and discovered repository evidence. `E2 documented / E3 code-derived` — `.cursor/workflow.config.yml:2-17`; `docs/modernization/legacy-map.md:195-198`.

## 2. Dependency inventory

| ID | Dependency | Version | Category | Used by | Support status | License | Evidence |
|----|------------|---------|----------|---------|----------------|---------|----------|
| DEP-001 | Intel Fortran Compiler Classic (`ifort`) and Intel runtime | Exact legacy version TBD; checked-in flags mention ifort 10/11/12-era linkage | runtime/compiler | Checked-in compiler selection, static builds, MKL path | unsupported — `ifort` was discontinued after 2024.2; legacy binary compatibility is unverified | Proprietary Intel toolchain/runtime | `E3 code-derived` — `include/options.inc:1-8`, `include/sfmake.inc:7-15`; `E2 documented` — [Intel 2026 compiler guide](https://www.intel.com/content/www/us/en/docs/fortran-compiler/developer-guide-reference/2026-0/overview.html) |
| DEP-002 | GNU Fortran (`gfortran`) and GNU runtime | TBD | runtime/compiler | Alternative setup/build path; branch-only macOS build work | supported upstream; exact legacy version and output compatibility unknown | GCC GPLv3 with runtime-library exception; not vendored | `E3 code-derived / E5 unknown` — `bin/setup_sf.sh:31-41`, `include/options.inc:9-16`; `docs/modernization/intent-ledger.md:27` |
| DEP-003 | Fortran module/static-library ABI (`.mod`, `libscifor.a`) | Compiler-specific; TBD | runtime/library | Downstream Fortran consumers, if any | unknown — no consumer inventory or trusted binary exists | Repository-level license absent; linked-library terms TBD | `E3 code-derived / E5 unknown` — `src/Makefile:4-20`, `etc/Makefile.template:1-41`; `docs/modernization/legacy-map.md:56,104,193` |
| DEP-004 | GNU Make build graph and include-path bootstrap | TBD | build | Core library, debug archive, and 17 utility targets | supported upstream; legacy graph is not reproducible from a clean shell | GPL external tool; Makefiles have no stated license | `E1 verified / E3 code-derived` — `src/Makefile:1-29`, `numutils/src/Makefile:1-94`; `docs/modernization/legacy-map.md:149,161` |
| DEP-005 | Archive/copy build tools (`ar`, `ranlib`, `rsync`, `sort`, `ls`, `mv`) | TBD | build | Static archives and module installation | supported on Unix-like hosts; versions and portability assumptions unknown | External tools; varied GPL/system licenses | `E3 code-derived` — `src/Makefile:8-20,37-43`; `bin/setup_sf.sh:111-163` |
| DEP-006 | Git revision stamping | Git version TBD; embedded hash is stale | build/tooling | `scifor_version.inc`, revision metadata | supported upstream; generated metadata lifecycle is defective | GPLv2 external tool | `E1 verified / E3 code-derived` — `src/Makefile:2,24-25`, `src/scifor_version.inc:1`; `docs/modernization/legacy-map.md:173` |
| DEP-007 | OpenMP compiler/runtime markers | OpenMP version/runtime TBD | runtime/concurrency | `ifort -openmp` flag and exported `omp_*` globals; no OpenMP API/directive use found | unknown — operational requirement not evidenced | Compiler/runtime-dependent; no vendored OpenMP code | `E3 code-derived / E5 unknown` — `include/options.inc:1-7`, `src/COMVARS.f90:59-67`; `docs/modernization/legacy-map.md:40,192` |
| DEP-008 | Bash setup and environment scripts | Bash version TBD | deploy/build | Interactive install, environment variables, library paths | supported upstream; scripts parse but setup path is broken and mutating | GPLv3 external shell; scripts have no stated license | `E1 verified / E3 code-derived` — `bin/setup_sf.sh:1-205`, `bin/sciforvars.sh:1-50`; `docs/modernization/legacy-map.md:49-50,150,162` |
| DEP-009 | Unix host command suite (`gzip`, `gunzip`, `mkdir`, `mv`, `chmod`, `rm`, `cp`, `ln`, `locate`) | TBD | runtime/deploy | Compressed files, lattice storage, generated plot scripts, setup | supported on Unix-like hosts; command status and quoting are not reliably handled | External tools; varied system licenses | `E3 code-derived` — `src/IOFILE.f90:219-292`, `src/TOOLS.f90:250-276`, `src/slplot_splot_3d.f90:38-68`, `numutils/src/ffcmplx.f90:29-35` |
| DEP-010 | Environment/path deployment (`LD_LIBRARY_PATH`, `LIBRARY_PATH`, `MANPATH`, `$HOME/.bin`) | Unversioned convention | deploy | Library discovery and CLI installation | unknown — no canonical deployment or target hosting model exists | Not applicable; operational convention | `E3 code-derived / E5 unknown` — `bin/sciforvars.sh:12-50`, `numutils/src/Makefile:1-8`; `docs/modernization/legacy-map.md:50,53` |
| DEP-011 | MPICH2 configuration and MPI-named shared state | MPICH2 1.4.1 path configured | runtime/concurrency | Environment config and `mpiID`/`mpiSIZE`-gated diagnostics; no MPI calls found | unsupported/obsolete — 1.4.1 released in 2011; actual runtime use unknown | MPICH license; binary/source absent from checkout | `E3 code-derived / E5 unknown` — `etc/library.conf:7-8`, `src/COMVARS.f90:59-67`; `E2 documented` — [MPICH 1.4.1 release](https://www.mpich.org/2011/08/24/mpich2-1-4-1-released-aug-24-2011/) |
| DEP-012 | BLAS | Version/implementation TBD | native/math | Matrix operations and fallback link path | unknown — required at link time, but no exact build/provider is present | Implementation/license TBD | `E3 code-derived / E5 unknown` — `include/sfmake.inc:7-28`, `src/MATRIX.f90:9-45`; `docs/modernization/legacy-map.md:33,133` |
| DEP-013 | LAPACK | Version/implementation TBD | native/math | Eigensystems, inversion, solve, SVD, determinant, covariance | unknown — required at link time, but exact ABI and routine behavior are unverified | Implementation/license TBD | `E3 code-derived / E5 unknown` — `include/sfmake.inc:7-28`, `src/MATRIX.f90:9-58,1283-1306`; `docs/modernization/legacy-map.md:33` |
| DEP-014 | Intel MKL BLAS/LAPACK/DFTI plus vendored interface files | Runtime version TBD; checked-in interfaces copyright 1999-2010 | native/math/FFT | Checked-in FFT default and optional math linkage | supported product; checked-in interface snapshot and production version unknown | Proprietary; `mkl_lapack.fi`, `mkl_dfti.f90`, and `mkl_trig_transforms.f90` state confidential material/no license grant | `E3 code-derived / E5 unknown` — `src/FFTGF_MKL.f90:1-43`, `src/mkl_lapack.fi:1-22`, `src/mkl_dfti.f90:1-22`, `include/sfmake.inc:7-15` |
| DEP-015 | FFTW3 native library and legacy Fortran include | FFTW3 release TBD | native/FFT | Alternative `FFTGF_FFTW3` backend and link configuration | supported upstream; not the checked-in default and never executed here | FFTW GPL; static-link/distribution impact requires legal review | `E3 code-derived / E5 unknown` — `src/FFTGF_FFTW3.f90:1-33`, `src/fftw3.f:1-30`, `include/sfmake.inc:24-29`; `docs/modernization/legacy-map.md:34,135` |
| DEP-016 | Numerical Recipes-derived radix-2 FFT implementation | Local snapshot; version/provenance TBD | vendored/native FFT | `FFTGF_NR`; branch-only baseline selects it | unknown — algorithm is present but production use and cross-backend behavior are unverified | No license grant found in file; provenance/legal review required | `E3 code-derived / E5 unknown` — `src/FFTGF_NR.f90:1-38`; `docs/modernization/intent-ledger.md:60,85` |
| DEP-017 | FFTPACK adapter (`zffti`, `zfftf`, `zfftb`) | Local wrapper version TBD; callee implementation absent | vendored/native FFT | Alternative `FFTGF_FFTPACK` backend | unsupported in this checkout — required symbols are not defined or linked | No license/provenance statement found in wrapper | `E3 code-derived` — `src/FFTGF_FFTPACK.f90:1-31`; whole-tree search found calls but no definitions; `docs/modernization/intent-ledger.md:28,56` |
| DEP-018 | OpenBLAS branch-only build dependency | Branch pin/version TBD | native/math/build | `initial-build` macOS reconstruction, not current `master` | supported upstream; not part of the current checked-out baseline | BSD-3-Clause upstream; branch acquisition/provenance must be audited | `E3 code-derived / E5 unknown` — `docs/modernization/intent-ledger.md:27,60,81` |
| DEP-019 | Bundled QUADPACK routines | Local revision TBD; 1983 algorithm references | vendored/numeric | `INTEGRATE` adaptive/fixed quadrature facade | unknown — snapshot has no update metadata and was not executed | License not stated in bundled file; provenance/legal review required | `E3 code-derived` — `src/integrate_d_quadpack.f90:1-16,193-217`, `src/Makefile:149-153` |
| DEP-020 | Bundled MINPACK F90 conversion | F77 original; F90 conversion modified 2010 | vendored/numeric | `OPTIMIZE` nonlinear solve and least-squares wrappers | unknown — vendored snapshot, no executable verification | GNU LGPL stated per routine; repository has no aggregate notices | `E3 code-derived` — `src/minpack.f90:37-55`, `src/WRAP_MINPACK.f90:1`, `src/OPTIMIZE.f90:1-59` |
| DEP-021 | Zhang/Jin/Burkardt special-function collection | Mixed routine dates; local revision TBD | vendored/numeric | `FUNCTIONS` public special-function surface | unknown — large mixed-provenance snapshot and exact supported surface unresolved | Per-routine acknowledgment permission; no single standard license or aggregate notice | `E3 code-derived` — `src/functions_special_funcs.f90:1-38,90-130`, `src/FUNCTIONS.f90:1-70,171` |
| DEP-022 | CHRPACK character/format conversion collection | Local snapshot TBD | vendored/library | Present in tree; no build or call reference found | unsupported/unmaintained snapshot; unused in mapped build | GNU LGPL statements inside routines; no aggregate notice | `E3 code-derived` — `src/CHRPACK.f90:337-345` and repeated licensing blocks; whole-tree build/reference search |
| DEP-023 | Burkardt spline interpolation routines | Local snapshot; routine dates vary | vendored/numeric | Included by `SPLINE` | unsupported as a vendored snapshot; no package/update path | GNU LGPL stated per routine | `E3 code-derived` — `src/SPLINE.f90:1-4`, `src/spline_interp.f90:29-37,102-110` |
| DEP-024 | Numerical Recipes-derived optimization/spline helpers | Local snapshots; versions/provenance TBD | vendored/numeric | Broyden/Brent and spline helper paths | unknown — active use exists, but provenance, supported surface, and legal route are unresolved | No complete license grant recovered; legal review required | `E3 code-derived / E5 unknown` — `src/Makefile:63-79`, `src/SPLINE.f90:1-4`, `src/spline_nr_mod.f90`; `docs/modernization/legacy-map.md:170` |
| DEP-025 | Missing `ZEROS` module | Absent at `master`/`5e4e6a3`; historical rename at `722cde3` | vendored/internal library | Imported by public `SCIFOR` facade | unsupported/absent — facade cannot resolve it from tracked source | License/provenance TBD because source is missing | `E3 code-derived / E5 unknown` — `src/SCIFOR.f90:1-17`, `src/Makefile:4-6`; `docs/modernization/intent-ledger.md:58,84` |
| DEP-026 | GNU `libmatheval` C API | Linked version TBD; latest upstream release identified as 1.1.11 (2008), not proven to be legacy version | tooling/native library | `func` expression-evaluation CLI | unsupported upstream/unmaintained; legacy ABI and grammar behavior unverified | GPLv3; native linking/distribution review required | `E3 code-derived / E5 unknown` — `numutils/src/func.f90:57-61`, `numutils/src/Makefile:44-46`; `E2 documented` — [FSF directory](https://directory.fsf.org/wiki/Libmatheval) |
| DEP-027 | Gnuplot process/script integration | Version TBD | plotting/tooling | `SLPLOT`/`splot` generated `.gp` shell scripts and manual plotting | supported upstream; availability and exact terminal behavior unverified | External gnuplot license; generated scripts are repository behavior | `E3 code-derived / E5 unknown` — `src/slplot_splot_3d.f90:17-68`, `numutils/src/splot.f90:119-155`; `docs/modernization/intent-ledger.md:21` |
| DEP-028 | DISLIN plus external `DLPLOT` Fortran module/library | Legacy versions TBD; DISLIN current upstream exists, `DLPLOT` source absent | plotting/native UI | Optional `vfplot` target omitted from default build | unsupported as assembled legacy dependency — `DLPLOT` is absent and linkage is commented out | DISLIN EULA; `DLPLOT` license/provenance TBD | `E3 code-derived / E5 unknown` — `numutils/src/vfplot.f90:1-6`, `include/sfmake.inc:53-61`; `E2 documented` — [DISLIN EULA/current project](https://www.dislin.de/eula.html) |
| DEP-029 | CLI argument and stdin/stdout protocol | Fortran runtime semantics; unversioned | integration/protocol | All 17 utility sources and shell pipelines | unknown — input errors, exact formatting, supported utility set, and ASP.NET process boundary are unresolved | Internal contract; repository license absent | `E2 documented / E3 code-derived / E5 unknown` — `src/PARSECMD.f90:36-61,87-191`, representative utilities at `numutils/src/deriv.f90:18-65`; `docs/modernization/legacy-map.md:129-130` |
| DEP-030 | Plain-text numeric/file formats, including per-surface complex-column conventions | Unversioned; compiler-dependent Fortran formatting | integration/data | CLI streams/files, SLPLOT/SLREAD, `pade`, `ffcmplx`, diagnostics | unknown — externally visible, but delimiter/precision/NaN/Infinity and complex ordering are not globally defined | Internal format; repository license absent | `E3 code-derived / E5 unknown` — `docs/modernization/legacy-map.md:113-123`; `docs/modernization/intent-ledger.md:24-25,89` |
| DEP-031 | Gzip-compressed text interoperability (`*.gz`) | gzip version TBD | integration/file | `IOFILE` automatic compression/expansion | supported externally; threshold and failure semantics unverified | gzip GPL external tool; data format is open | `E3 code-derived` — `src/IOFILE.f90:174-203,219-268`; `docs/modernization/legacy-map.md:115` |
| DEP-032 | Filesystem side-effect conventions | Host filesystem semantics; unversioned | integration/file | Sibling output replacement, lattice directories, plot scripts, convergence/error files | unknown — path quoting, permissions, overwrite, and failure behavior are incompletely handled | Internal behavior; repository license absent | `E3 code-derived` — `src/IOFILE.f90:130-300`, `src/TOOLS.f90:250-276`, `numutils/src/ffcmplx.f90:29-61`; `docs/modernization/legacy-map.md:131-132` |

## 3. Target substitution decisions

| DEP ID | Target equivalent | Decision | Impedance mismatch | ADR needed? | Verification |
|--------|-------------------|----------|--------------------|-------------|--------------|
| DEP-001 | .NET 8 SDK, C# compiler, and CLR | substitute | Fortran kinds, array layout, floating-point flags, static initialization, STOP/error behavior, and formatted I/O differ | yes — target runtime/numeric baseline | Compile/run trusted `ifort` fixtures if recoverable; compare routine and CLI outputs at behavior-specific tolerances |
| DEP-002 | .NET 8 SDK, C# compiler, and CLR | substitute | Same language/runtime mismatch as DEP-001; GNU flags and formatting can produce different legacy outputs | yes — target runtime/numeric baseline | Pin legacy `gfortran`, execute the same corpus, and detect compiler-dependent output before accepting one oracle |
| DEP-003 | TBD: managed library API, ASP.NET endpoints, CLI compatibility layer, or native adapter | blocked | Fortran module ABI, allocatable arrays, callbacks, column-major arrays, and unknown external consumers prevent choosing wrap versus replace | yes — public/process boundary | Obtain consumer manifests; compile representative consumers; contract-test every accepted boundary |
| DEP-004 | `dotnet build` / MSBuild and target CI | reimplement | Make targets encode generated revision files, dual debug/optimized archives, include paths, and shell side effects | yes — build/release strategy | Reproducible clean build in CI; source/version metadata test; no host aliases or untracked inputs |
| DEP-005 | MSBuild/.NET artifact generation and managed file-copy tasks | reimplement | Static archives and compiler-specific `.mod` files have no managed analogue | no — subordinate to build strategy | Clean build produces only declared artifacts; installation manifest test |
| DEP-006 | Assembly informational version plus CI-injected source revision | substitute | Legacy metadata is mutable, checked in, and can become stale | no | Build twice from a pinned commit and assert runtime-reported revision equals source revision |
| DEP-007 | TBD: .NET task/thread model or deliberate single-threaded execution | blocked | Shared globals are visible, but no OpenMP calls establish scheduling, determinism, or required parallel speedup | yes — concurrency model | Confirm downstream use; deterministic single/concurrent fixture runs; load test only after semantics are fixed |
| DEP-008 | Managed configuration, `dotnet publish`, and target deployment automation | reimplement | Interactive prompts, sourced environment mutation, symlinks, and in-repo writes conflict with repeatable deployment | yes — deployment/configuration | Non-interactive clean deployment in an isolated environment; immutable-artifact and configuration tests |
| DEP-009 | `System.IO`, `System.IO.Compression`, and explicit process adapters only where retained | reimplement | Shell quoting, exit status, permissions, globbing, and platform behavior differ | yes — host/process integration policy | Disposable filesystem integration tests covering spaces, missing tools, permissions, failures, and rollback |
| DEP-010 | TBD: framework-dependent deployment, service, CLI tool, or container layout | blocked | ASP.NET Core target does not by itself decide service versus CLI topology or native-library discovery | yes — deployment and process boundary | Deploy to selected target from scratch; startup/configuration smoke tests; no user-home dependency |
| DEP-011 | TBD: remove MPI state, in-process partition context, or supported MPI adapter | blocked | Existing code gates output by mutable `mpiID` without making MPI calls; external setters/consumers are unknown | yes — concurrency/process boundary | Consumer inventory plus single-/multi-partition behavior fixtures; prove absence before removal |
| DEP-012 | TBD .NET linear-algebra provider or independently implemented narrow kernels | blocked | BLAS layout/transposition, provider threading, determinism, and native lifecycle differ | yes — numeric provider | Inventory called BLAS symbols; provider micro-fixtures, ill-conditioned matrices, concurrency, and performance evidence |
| DEP-013 | TBD .NET linear-algebra provider or native LAPACK adapter | blocked | LAPACK workspace, INFO codes, eigenvector ordering/sign, pivoting, and column-major semantics differ | yes — numeric provider | Symbol-level contract matrix; golden/identity/residual tests and failure-code mapping |
| DEP-014 | TBD: supported MKL native adapter or non-MKL provider; checked-in Intel files cannot be port inputs without authorization | blocked | Production use/version unknown; proprietary headers; descriptor lifecycle, scaling, threading, and DFT conventions differ | yes — MKL disposition and FFT provider | Legal clearance; production build evidence; cross-backend sign/normalization fixtures and descriptor failure tests |
| DEP-015 | TBD: FFTW native wrapper or approved managed FFT provider | blocked | Backend not established; plan lifecycle, normalization, direction signs, memory layout, and GPL/static-link implications differ | yes — FFT backend and licensing | Recover production backend/version; cross-backend corpus for impulses, tones, round trips, Matsubara transforms, and errors |
| DEP-016 | TBD approved FFT implementation | blocked | Radix-2 restriction, sign/index shifts, in-place mutation, and unverified provenance prevent approval | yes — FFT backend and licensing | Legal/provenance decision; powers-of-two and rejected lengths; compare against trusted production FFT fixtures |
| DEP-017 | TBD approved FFT implementation | blocked | Referenced FFTPACK symbols are absent, so the legacy backend cannot currently be built or characterized | yes — FFT backend and missing-source disposition | Recover exact callee source/version, build it, and run cross-backend fixtures; otherwise exclude by explicit scope decision |
| DEP-018 | TBD .NET/native linear-algebra provider | blocked | Branch-only evidence cannot establish current baseline or accepted numerical behavior | yes — baseline and numeric provider | User baseline decision; audit branch build script and recapture outputs from an accepted oracle |
| DEP-019 | C# implementation of the accepted `INTEGRATE` facade using independently sourced algorithms | reimplement | Callback model, workspace, status/error codes, stopping criteria, and floating-point paths differ | yes — integration algorithm/provenance | Characterize each exposed integration mode; singular/oscillatory/infinite cases; error estimate/status and tolerance tests |
| DEP-020 | C# implementation of the accepted `OPTIMIZE` facade using independently sourced algorithms | reimplement | Callback/Jacobian contracts, finite-difference steps, convergence codes, workspace, and scaling differ | yes — solver algorithm/provenance | Root/least-squares corpus including ill-conditioned and non-convergent cases; map status and iteration semantics |
| DEP-021 | TBD approved special-function provider or bounded independent implementation | blocked | Large unresolved public surface, branch cuts, overflow domains, error behavior, accuracy, and non-standard licensing terms | yes — special-function scope/provider | API usage inventory; legal decision; reference-value and boundary corpus per retained function |
| DEP-022 | None in target | drop | No mapped build or call surface; retaining unused LGPL source increases provenance scope | no | Whole-target reference scan and accepted API inventory confirm no required CHRPACK surface |
| DEP-023 | C# implementation of the accepted spline facade | reimplement | Boundary conditions, interpolation/extrapolation, indexing, and floating-point paths differ | yes — spline algorithm/provenance | Knot/boundary/extrapolation fixtures, continuity checks, and retained API contract tests |
| DEP-024 | TBD approved optimization/spline implementation | blocked | Active surface and source lineage are not fully separated from other local algorithms; legal and numeric equivalence are unresolved | yes — vendored-code disposition | Call-graph inventory, provenance review, and fixture corpus per retained routine before selecting provider or rewrite |
| DEP-025 | TBD: recover `ZEROS`, confirm `OPTIMIZE` replacement, or remove facade member | blocked | Source/history/facade disagree; choosing one changes the public API and possibly root-finding behavior | yes — authoritative facade/API | Baseline and consumer decision; compile facade; contract tests for every retained `ZEROS`/`OPTIMIZE` entrypoint |
| DEP-026 | TBD managed expression parser/evaluator with an explicit allowed grammar | blocked | Function/operator grammar, precedence, error handling, locale, native ABI, GPL obligations, and user-input security differ | yes — expression language/provider/security | Capture accepted/rejected expression corpus; differential numeric tests; fuzz/resource-limit/security tests |
| DEP-027 | External Gnuplot process behind a narrow adapter | wrap | Script syntax, terminal selection, process lifecycle, filesystem access, and server-side execution security differ | yes — plotting/process integration | Golden script/data fixtures; real Gnuplot integration test in isolated deployment; timeout/exit/security tests |
| DEP-028 | TBD: recover/wrap `DLPLOT`+DISLIN or replace the `vfplot` surface | blocked | Missing module/API, omitted default target, native UI lifecycle, X11 linkage, and supported-surface uncertainty | yes — plotting UI and `vfplot` scope | User scope decision; recover source/version/license; build/run representative vector plots or explicitly retire behavior |
| DEP-029 | TBD: compatibility CLI, ASP.NET API, or both | blocked | Process arguments/stdin EOF and streaming semantics do not map automatically to HTTP request/response lifecycle | yes — integration/process boundary | Decide supported utilities; byte-level CLI fixtures and/or API contract tests; malformed input, streaming, cancellation, and exit mapping |
| DEP-030 | Explicit per-surface .NET text codecs | reimplement | List-directed formatting, locale, exponent spelling, precision, NaN/Infinity, and `(Re,Im)` versus `(Im,Re)` vary | yes — file/data contracts | Golden fixtures per surface and compiler; locale matrix; round trips; user decisions for complex ordering and exactness |
| DEP-031 | `System.IO.Compression.GZipStream` | substitute | Legacy expands in place and recompresses separately; timestamps, overwrite, threshold, and failure behavior may differ | yes — compressed-file contract | Real gzip interoperability; threshold/overwrite/corruption/permission fixtures; preserve source on failure |
| DEP-032 | `System.IO` behind explicit file-storage adapters | reimplement | Legacy deletes/overwrites/moves implicitly, ignores many exit statuses, and uses process-global working directories | yes — filesystem ownership/security | Contract and real-filesystem integration tests for naming, overwrite, atomicity, permissions, path traversal, and partial failure |

## 4. Obsolete or vulnerable dependencies

| DEP ID | Issue | Severity | Containment needed | Mitigation |
|--------|-------|----------|--------------------|------------|
| DEP-001 | `ifort` discontinued after 2024.2; macOS support had already been removed | high | yes | Use only in an isolated legacy-oracle environment if an exact trusted version is recovered; do not make it target tooling. |
| DEP-003 | Compiler-specific static/module ABI with unknown external consumers and no reproducible build | high | yes | Do not expose or retire it until consumer inventory and boundary ADR exist. |
| DEP-008 | Interactive script writes configuration, assumes untracked directory layout, and contains compiler/filename/source-script defects | high | yes | Never run against the read-only baseline; reconstruct in disposable containment only. |
| DEP-011 | MPICH2 1.4.1 path is from 2011 and no actual MPI linkage/calls are evidenced | high | yes | Do not deploy it; establish whether MPI-named state is required, then retire or map deliberately. |
| DEP-014 | Checked-in Intel files state confidential/proprietary material and no license grant | critical | yes | Exclude from target source inputs pending legal authorization; prefer public API declarations or an independently licensed provider after ADR. |
| DEP-015 | FFTW version unknown; static-link path can create GPL distribution obligations | high | yes | Pin version and linking model, obtain legal review, and do not distribute until approved. |
| DEP-017 | FFTPACK wrapper calls missing symbols and has no recovered provenance | high | yes | Recover exact source/license or exclude the backend by decision. |
| DEP-021 | Special-function collection has mixed acknowledgment-only terms and no aggregate license | high | yes | Legal/provenance review; independently implement only retained functions from approved specifications if permitted. |
| DEP-022 | Large unused LGPL CHRPACK snapshot broadens provenance surface | medium | no | Drop after confirming no supported public/consumer dependency. |
| DEP-023 | Vendored LGPL spline snapshot has no package/update path or notices file | medium | yes | Reimplement accepted facade from approved sources and retain license notices for any reused material. |
| DEP-024 | Numerical Recipes-derived sources have unresolved license/provenance | high | yes | Do not translate or redistribute until legal disposition and retained surface are explicit. |
| DEP-025 | Required facade module is absent | critical | yes | Recover source or decide `OPTIMIZE`/removal before compiling or planning the public API. |
| DEP-026 | `libmatheval` upstream is unmaintained; native GPL dependency processes user-supplied expressions without evidenced error controls | high | yes | Disable exposure outside containment; select an approved grammar/provider only after security and license decision. |
| DEP-028 | `DLPLOT` source/license absent and DISLIN linkage is commented out | high | yes | Treat `vfplot` as unavailable until exact dependency chain and product scope are recovered. |

No dependency-specific CVE claim is made because exact legacy versions are not known. Version recovery is required before vulnerability scanning can produce reliable results.

## 5. Blockers

| DEP ID | Why blocked | Affected behavior | Required decision |
|--------|-------------|-------------------|-------------------|
| DEP-003 | Unknown downstream Fortran consumers prevent choosing a managed API, compatibility CLI, native wrapper, or retirement | Library API; `BEH-*` TBD; INT-002 | Identify all consumers and approve the target process/public API boundary. |
| DEP-007 | OpenMP is configured but no runtime use is evidenced; shared `omp_*` state remains public | Concurrency/determinism; `BEH-*` TBD | Confirm whether parallel execution is required and define target concurrency semantics. |
| DEP-010 | No canonical legacy deployment and no selected target hosting topology | All deployable surfaces; `BEH-*` TBD | Choose CLI, ASP.NET service, hybrid, and deployment environment. |
| DEP-011 | Obsolete MPICH path plus MPI-named state but no MPI calls | Diagnostics/convergence gating; `BEH-*` TBD | Decide whether MPI behavior exists outside the checkout and whether to remove or adapt it. |
| DEP-012 | BLAS implementation/version and exact retained symbol surface are unknown | Matrix API; `BEH-*` TBD; INT-002 | Recover production provider and approve a .NET/native linear-algebra route. |
| DEP-013 | LAPACK implementation/version and error/eigen semantics are uncharacterized | Matrix API; `BEH-*` TBD; INT-002 | Inventory called routines and approve provider, ABI, and numeric verification criteria. |
| DEP-014 | Production MKL role/version and legal right to use checked-in Intel interfaces are unresolved | Matrix and FFT; `BEH-*` TBD; INT-006 | Obtain legal disposition and production build evidence; select one FFT/math provider. |
| DEP-015 | FFTW is an alternative, not an established production backend | FFT transforms; `BEH-*` TBD; INT-006 | Identify production backend/version and approve linking/license model. |
| DEP-016 | Numerical Recipes backend has unverified production use and unresolved provenance | FFT transforms; `BEH-*` TBD; INT-006 | Resolve baseline/backend and legal provenance before any translation. |
| DEP-017 | FFTPACK implementation is incomplete in the checkout | FFT transforms; `BEH-*` TBD; INT-016 | Recover exact callee source/version/license or explicitly exclude the backend. |
| DEP-018 | OpenBLAS exists only in conflicting branch evidence | Matrix/build oracle; `BEH-*` TBD; INT-015 | Select authoritative branch/baseline and disposition `initial-build`. |
| DEP-021 | Special-function scope, licensing, and acceptable provider are unresolved | FUNCTIONS API; `BEH-*` TBD; INT-002 | Name retained functions, obtain legal approval, and select provider/reimplementation route. |
| DEP-024 | Active vendored numeric helper lineage and supported surface are unresolved | OPTIMIZE/SPLINE; `BEH-*` TBD | Complete provenance/call-surface review and approve algorithm sources. |
| DEP-025 | Facade imports missing `ZEROS` while history suggests `OPTIMIZE` | Public facade; `BEH-*` TBD; INT-002 | Select authoritative facade and recover consumer expectations. |
| DEP-026 | Expression grammar/provider/security and utility support are not decided | `func`; `BEH-*` TBD; INT-003 | Confirm `func` scope and approve an expression grammar, provider, license, and threat model. |
| DEP-028 | `DLPLOT` is missing and `vfplot` is omitted from default build | `vfplot`; `BEH-*` TBD | Confirm whether `vfplot` is supported; recover dependency or approve retirement/replacement. |
| DEP-029 | ASP.NET target does not decide whether legacy CLI/stdin/stdout compatibility survives | All CLI utilities; `BEH-*` TBD; INT-003–INT-009 | Choose process/integration model and supported utility set. |

## 6. Links

- Assessment: `docs/modernization/ASSESSMENT.md` (not created yet)
- Translation gaps: `docs/modernization/translation-gaps.md` (not created yet)
- ADRs: `docs/decisions/` (no ADRs created by this phase)

*Created: 2026-08-09*
