# Legacy Map

## 1. Scope and coverage

- Legacy system: SciFortran, a Fortran numerical library plus command-line numerical utilities. `E2 documented` — `README.md:1`; `E3 code-derived` — `src/SCIFOR.f90:1-17`, `src/Makefile:4-20`, `numutils/src/Makefile:8-86`.
- Repository inspected: `/Users/philipteitel/code/ADD-migrations/sci-fortran-legacy`, at Git revision `5e4e6a3938a7ef6e1163a34b3236be7ea7eb22f9` on `2026-08-09`. `E1 verified` — `git -C <legacy-repo> rev-parse HEAD`.
- Coverage summary: all 132 tracked paths were inventoried; all 98 Fortran source/include files were structurally scanned; build/configuration files, all 17 CLI program sources, and all 15 `numutils/test/` assets were inspected. `E1 verified` — repository inventory using `git ls-files` and extension/path counts on `2026-08-09`.
- Read-only check: no legacy write was performed during this mapping run. Final status showed two checkout-local untracked configuration files, `numutils/src/options.inc` and `numutils/src/sfmake.inc`; they were inspected but are not part of revision `5e4e6a...`. `E1 verified` — `git -C <legacy-repo> status --short --untracked-files=all`.

| Area | Coverage | Evidence grade | Notes / exclusions |
|---|---|---:|---|
| Root metadata and documentation | Deep | E2 documented / E3 code-derived | `README.md:1` is the only tracked root documentation file found; it identifies the checkout as an experiment cloned from an upstream GitHub repository. |
| Build, setup, and environment configuration | Deep | E1 verified / E3 code-derived | Inspected `src/Makefile`, `numutils/src/Makefile`, tracked `include/options.inc`/`include/sfmake.inc`, checkout-local untracked `numutils/src/options.inc`/`numutils/src/sfmake.inc`, `etc/library.conf`, `etc/opt.mk`, `etc/Makefile.template`, `bin/setup_sf.sh`, and `bin/sciforvars.sh`. Shell syntax checks passed; clean-shell dry-run builds did not. |
| Core library source (`src/`) | Deep | E3 code-derived | All 81 Fortran files were structurally scanned. Facade, common services, I/O, data structures, integration, matrix, FFT, optimization, Green-function, lattice, random, spline, statistics, and tool modules received targeted content inspection. |
| Bundled numerical and compatibility source | Light | E3 code-derived | Headers, provenance comments, language constructs, call sites, and size were inspected for `CHRPACK.f90`, `functions_special_funcs.f90`, `integrate_d_quadpack.f90`, `minpack.f90`, `fftw3.f`, and `mkl_lapack.fi`; these very large files were not read line-by-line. |
| CLI utilities (`numutils/src/`) | Deep | E3 code-derived | All 17 `PROGRAM` entrypoints were inspected, including argument parsing, standard-input/output behavior, file side effects, and external calls. |
| Test/fixture assets (`numutils/test/`) | Deep | E3 code-derived / E4 inferred | All 15 tracked assets were inventoried; numeric data and plotting artifacts were inspected. No test driver, assertions, or provenance document was found. The 79k-line Grace project `pdf.dat$` was sampled rather than read line-by-line. |
| Generated/output directories (`.bin/`, `lib/`, `src/mods/`, `src/debug_mods/`, `src/objs/`, `src/debug_objs/`) | Deep | E1 verified | Directories exist but contained no build products during inspection on `2026-08-09`. |
| Git history beyond current revision | Skipped | E5 unknown | `/map-legacy` inventories the current legacy tree; intent-bearing commit and tag history is reserved for `/mine-history`. |
| Executable behavior | Unknown | E5 unknown | No legacy binary was present, and the checked-out build could not be dry-run from a clean shell without reconstructing its setup/symlink environment. Interactive `setup_sf.sh` was not executed because it writes into the read-only legacy repository. |
| Databases, services, schedulers, web UI, and deployment manifests | Deep | E3 code-derived | Whole-tree inventory and targeted searches found no database schema/client, HTTP/socket service, scheduler manifest, container/orchestration file, or graphical/web UI implementation. Absence is limited to this checkout. |

## 2. Technology and runtime inventory

| Component | Language / runtime / framework | Version | Support status | Evidence grade | Source or pointer |
|---|---|---|---|---:|---|
| Core library | Free-form Fortran modules and included procedures | Exact language level TBD; comments identify F90/F95 while command-line code uses Fortran 2003-era argument intrinsics | Compiler acceptance not verified | E2 documented / E3 code-derived / E5 unknown | `src/IOTOOLS.f90:1-10`; `src/PARSECMD.f90:36-61`; `src/SCIFOR.f90:1-17` |
| FFTW constants include | Fixed-form Fortran include content | FFTW3 API constants; exact FFTW release TBD | Build use not verified | E3 code-derived / E5 unknown | `src/fftw3.f:1-30`; `src/FFTGF_FFTW3.f90:83-95` |
| Production compiler choice | GNU `gfortran` or Intel `ifort` | Exact versions TBD | Both are named in setup; neither was verified for this revision | E2 documented / E3 code-derived / E5 unknown | `bin/setup_sf.sh:31-41`; `include/options.inc:1-16`; `etc/library.conf:1-9` |
| Available compiler configuration | Tracked options support `ifort`/`gfortran`; checked-in environment selects `ifort`; the CLI directory also has an untracked local copy | Version TBD | Machine-specific and unverified | E3 code-derived / E5 unknown | `include/options.inc:1-16`; `etc/library.conf:1-9`; checkout-local `numutils/src/options.inc:1-16` |
| Build system | GNU Make plus shell/environment include-path bootstrap | Makefile dialect; minimum/version TBD | Clean-shell dry-run currently blocked | E1 verified / E3 code-derived | `src/Makefile:1-29`; `numutils/src/Makefile:1-94`; `etc/library.conf:9` |
| Setup/environment runtime | Bash plus Unix tools (`ln`, `cp`, `make`, `git`, `gzip`, `gunzip`, `mkdir`, `mv`, `rsync`, `ar`, `ranlib`) | Bash/tool versions TBD | `bash -n` passed; setup was not executed | E1 verified / E3 code-derived | `bin/setup_sf.sh:1-205`; `bin/sciforvars.sh:1-50`; `src/Makefile:8-20`; `src/IOFILE.f90:219-292` |
| Linear algebra | BLAS and LAPACK, either local/static or MKL-backed | Versions TBD | Required by link configuration; not linked in this checkout | E3 code-derived / E5 unknown | `include/sfmake.inc:7-28`; `bin/setup_sf.sh:111-140`; `src/MATRIX.f90:9-45` |
| Fourier transforms | Intel MKL DFT is checked-in default; FFTW3 and Numerical Recipes implementations are alternatives | MKL/FFTW versions TBD | Backend selection is inconsistent; no backend executed | E3 code-derived / E5 unknown | `src/FFTGF.f90:1-8`; `src/FFTGF_MKL.f90:23-54`; `src/FFTGF_FFTW3.f90:83-95`; `src/FFTGF_NR.f90:1-35`; `bin/setup_sf.sh:193-202` |
| Numerical integration | SciFortran `INTEGRATE` facade with bundled QUADPACK routines | QUADPACK provenance documented in source; local revision TBD | Not executed | E3 code-derived | `src/INTEGRATE.f90:1-55`; `src/integrate_d_quadpack.f90:1-16,193-217` |
| Nonlinear solve / least squares | SciFortran `OPTIMIZE` module with bundled MINPACK-derived routines | F77 original / F90 conversion dated 2010 in source comments | Not executed | E3 code-derived | `src/OPTIMIZE.f90:1-53`; `src/minpack.f90:37-55` |
| Special functions | Large bundled F77/F90-derived special-function collection | Individual routines carry differing dates/provenance | Provenance and redistribution terms require review | E3 code-derived | `src/functions_special_funcs.f90:1-38,90-130` |
| Expression evaluator | `libmatheval` C API called from Fortran | Version TBD | External runtime/library not checked in or linked | E3 code-derived / E5 unknown | `numutils/src/func.f90:57-61,83-86`; `numutils/src/Makefile:44-46` |
| Plotting | Gnuplot script generation; external `DLPLOT`/DISLIN dependency for vector plots | Versions TBD | Gnuplot/DISLIN availability not verified; `DLPLOT` source is absent | E3 code-derived / E5 unknown | `numutils/src/splot.f90:22-47,119-155`; `numutils/src/vfplot.f90:1-6`; `bin/sciforvars.sh:54-63` |
| Parallelism markers | OpenMP compile flags and common OpenMP-named globals; MPI-named configuration variables | APIs/versions TBD | No MPI/OpenMP calls were found in the scanned Fortran source; intended behavior unknown | E3 code-derived / E5 unknown | `include/options.inc:1-7`; `src/COMVARS.f90:59-67`; `etc/library.conf:7-8` |
| Web/application framework | None found | Not applicable | Not evidenced in this checkout | E3 code-derived | Whole-tree inventory; CLI and static-library build definitions at `src/Makefile:4-20` and `numutils/src/Makefile:8-86` |

Precision/type note: the public numeric API is dominated by `real(8)`, `complex(8)`, and kind-8 constants. `COMMON_VARS` also declares `ddp=16` and quad-range constants, but a whole-source scan found no explicit `real(16)`/`complex(16)` declaration. These numeric kind values are implementation selections rather than documented portable precision contracts. `E3 code-derived` — `src/COMVARS.f90:13-39`; representative kind-8 declarations at `src/PARSECMD.f90:130-149`. Exact compiler-dependent widths and tolerances remain `E5 unknown`.

## 3. Build and operational entrypoints

| Entrypoint | Purpose | Inputs | Outputs / side effects | Works today | Evidence grade | Source or pointer |
|---|---|---|---|---|---:|---|
| `bin/setup_sf.sh` | Interactive installation attempt; chooses compiler/MKL, writes configuration, builds local BLAS/LAPACK/FFTW, and selects an FFT source | User prompts plus an expected parent layout containing `sf/` and `local/` | Writes `etc/opt.mk` and `etc/library.conf`, builds dependency trees, creates links, and invokes the core make | Not run; unsafe for read-only legacy checkout and contains blocking path/name defects | E1 verified / E3 code-derived | `bin/setup_sf.sh:1-15,31-70,80-105,111-205` |
| `source bin/sciforvars.sh` | Establishes SciFortran paths and compiler/library environment | `SFDIR` argument/environment and `etc/library.conf` | Exports library/include/bin paths and changes `LD_LIBRARY_PATH`, `MANPATH`, `LIBRARY_PATH` | Not verified; depends on configured paths and an undefined `SMKL` shell variable | E3 code-derived / E5 unknown | `bin/sciforvars.sh:1-50` |
| `make` in `src/` | Builds optimized/debug modules and both static SciFortran archives | `options.inc`, compiler, generated directories, `SFINCLUDE`, `SFLIB`, and module dependencies | Optimized/debug objects/modules, `libscifor.a`, `libscifor_deb.a`, and rewritten version include | No — `make -n` stops because local `src/options.inc` is absent unless an include search path is injected | E1 verified / E3 code-derived | `src/Makefile:1-25,32-315`; `etc/library.conf:9` |
| `make version` in `src/` | Embeds current Git revision in Fortran include | Git executable and repository metadata | Rewrites `src/scifor_version.inc` | Not run because it mutates the legacy checkout | E3 code-derived | `src/Makefile:2,24-25`; `src/scifor_version.inc:1` |
| `make` in `numutils/src/` | Builds the default CLI utility set | Configured compiler, SciFortran archive/modules, external libraries, and checkout-local include files | Executables under `$(HOME)/.bin` | No clean-checkout build verified; its required local `sfmake.inc`/`options.inc` are untracked | E1 verified / E3 code-derived | `numutils/src/Makefile:1-8,16-91`; checkout-local `numutils/src/sfmake.inc:1-33` and `numutils/src/options.inc:1-16` |
| Named utility targets in `numutils/src/Makefile` | Build individual CLI programs, including two omitted from default `all` | Same as above; target-specific sources/libraries | One executable per target under `$(HOME)/.bin` | Unknown; no built executables were found in the repository | E3 code-derived / E5 unknown | `numutils/src/Makefile:16-91` |
| `etc/Makefile.template` | Template for downstream Fortran programs linking SciFortran | User program source, `$(SFDIR)/etc/lib.mk`, compiler flags | Optimized or debug executable | Unknown; referenced `etc/lib.mk` is not tracked | E3 code-derived / E5 unknown | `etc/Makefile.template:1-41`; tracked-file inventory |
| Installed static library | Linkable SciFortran archives | Compiled core modules and external math libraries | `libscifor.a` and `libscifor_deb.a` moved to `$(SFLIB)` | Not present in checkout | E1 verified / E3 code-derived | `src/Makefile:8-20` |
| Operational service/job entrypoint | Long-running service, scheduled task, daemon, or server | TBD | TBD | None found | E3 code-derived | Whole-tree inventory; all detected executable entrypoints are Fortran `PROGRAM`s under `numutils/src/` |

The default CLI build list is `deriv kdensity numstat splot func wmatsubara fftgf arange pade logspace linspace fermi spline random histogram`; `ffcmplx` and `vfplot` have sources/targets but are omitted from `all`. `E3 code-derived` — `numutils/src/Makefile:8,24-26,84-86`.

## 4. Application topology

| Unit / module | Responsibility | Depends on | Exposes / called by | Deployable unit | Evidence grade | Source or pointer |
|---|---|---|---|---|---:|---|
| `SCIFOR` | Umbrella module that re-exports the primary library modules | `COMMON_VARS`, `TIMER`, `PARSE_CMD`, `FFTGF`, `FUNCTIONS`, `GREENFUNX`, `INTEGRATE`, `IOTOOLS`, `MATRIX`, `RANDOM`, `SPLINE`, `TOOLS`, `STATISTICS`, `ZEROS` | Downstream Fortran callers | `libscifor.a` | E3 code-derived | `src/SCIFOR.f90:1-17` |
| `COMMON_VARS` | Shared constants, version/timestamp functions, console messaging, ANSI formatting, and MPI/OpenMP-named state | Generated `scifor_version.inc` | Most core modules and utilities | Static library module | E3 code-derived | `src/COMVARS.f90:9-39,59-103,112-124,189-254` |
| `PARSE_CMD` | Registers typed command variables, prints help, and reads command-line arguments | `COMMON_VARS`; `command_argument_count` / `get_command_argument` | Most CLI utilities | Static library module | E3 code-derived | `src/PARSECMD.f90:1-28,36-61,87-100,109-191` |
| `IOTOOLS` / `IOFILE` | `IOTOOLS` aggregates `IOFILE`, `SLPLOT`, and `SLREAD`; `IOFILE` provides filesystem metadata, gzip-aware handling, and directory creation | `COMMON_VARS`; shell commands | Library callers and file-processing utilities | Static library modules | E3 code-derived | `src/IOTOOLS.f90:1-10`; `src/IOFILE.f90:130-203,219-300` |
| List modules | Dynamic ordered/unordered real/complex lists used to accumulate unknown-size input | Internal node/list types | Stream-processing CLI utilities | Static library modules | E3 code-derived | `src/LIST_D_UNORDERED.f90:1-75`; `src/LIST_D_ORDERED.f90:1-90`; `src/LIST_Z_UNORDERED.f90:1-74` |
| `FUNCTIONS` / `SPLINE` / `TOOLS` | Special functions, interpolation, comparisons, sorting/searching, grids, derivatives, and general numerical helpers | `COMMON_VARS`; large included routine collections | Core library and many utilities | Static library modules | E3 code-derived | `src/FUNCTIONS.f90:1-70`; `src/SPLINE.f90:1-74`; `src/TOOLS.f90:1-80` |
| `INTEGRATE` | Quadrature facade for adaptive and fixed numerical integration | `COMMON_VARS`, `TOOLS`, included integration wrappers, bundled QUADPACK | Library callers | Static library module | E3 code-derived | `src/INTEGRATE.f90:1-55`; `src/integrate_d_quadpack.f90:1-16` |
| `MATRIX` | Matrix operations and LAPACK-facing algorithms: eigensystems, inversion, solve, SVD, determinant, covariance, tensor product | `COMMON_VARS`, `TOOLS`, BLAS/LAPACK | Library callers | Static library module | E3 code-derived | `src/MATRIX.f90:1-58,130-156` |
| `OPTIMIZE` | Multidimensional root solving and least-squares interfaces | `COMMON_VARS`, `TOOLS`, included optimization routines, bundled MINPACK | Library callers | Static library module | E3 code-derived | `src/OPTIMIZE.f90:1-59,61-101`; `src/minpack.f90:45-55` |
| `FFTGF` | Generic Fourier/trigonometric transform interface with selected backend implementation | `COMMON_VARS`; MKL DFT by checked-in include selection | Core callers and `fftgf` utility | Static library module | E3 code-derived | `src/FFTGF.f90:1-54`; `src/FFTGF_MKL.f90:23-54` |
| `GREENFUNX` | Derived types and operations for Matsubara, real-frequency, and equilibrium Keldysh Green-function arrays | `COMMON_VARS`, included allocation/identity procedures | Library callers | Static library module | E3 code-derived | `src/GREENFUNX.f90:1-91,93-170` |
| `SQUARE_LATTICE` | Square-lattice density-of-states, dispersion, velocity, and momentum-grid helpers | `COMMON_VARS`, `INTEGRATE`, `TOOLS`, `IOTOOLS`, `FUNCTIONS` | Library callers | Static library module | E3 code-derived | `src/SQUARE_LATTICE.f90:1-58` |
| `PADE` | Padé analytic continuation from imaginary/Matsubara to real-frequency data | `COMMON_VARS`, `TOOLS` | `pade` utility and library callers | Static library module | E3 code-derived | `src/PADE.f90:1-36,38-54`; `numutils/src/pade.f90:1-12` |
| `RANDOM` / `STATISTICS` | Random sampling, clock-based intrinsic RNG seeding, histograms, moments, covariance, and kernel density estimation | `COMMON_VARS`, `TOOLS`, intrinsic `RANDOM_SEED`/`SYSTEM_CLOCK` | Library and statistics/random CLI utilities | Static library modules | E3 code-derived | `src/RANDOM.f90:1-52,197-208`; `src/STATISTICS.f90:1-50` |
| Plot integrations (`SLPLOT`, external `DLPLOT`) | `SLPLOT` generates Gnuplot data/scripts; `vfplot` imports a non-tracked `DLPLOT` module associated with external DISLIN configuration | `COMMON_VARS`, `IOFILE`, host shell; external DLPLOT/DISLIN for `vfplot` | `splot`, `vfplot` | Static library module plus CLI; `vfplot` dependency unavailable | E3 code-derived / E5 unknown | `src/SLPLOT.f90:1-63`; `src/slplot_splot_3d.f90:1-68`; `numutils/src/vfplot.f90:1-6`; `bin/sciforvars.sh:54-63` |
| CLI utility layer | 17 independent stdin/stdout or file-driven programs | `PARSE_CMD` and selected core modules; some external tools/libraries | Shell users and pipelines | Individual native executables | E3 code-derived | `numutils/src/*.f90`; build mapping at `numutils/src/Makefile:16-86` |
| Missing `ZEROS` dependency | Facade imports a zero/root-finding module whose source is absent | Source not present; default `allmod` also has no `ZEROS` target | `SCIFOR` | Intended static library module | E3 code-derived / E5 unknown | `src/SCIFOR.f90:1-17`; `src/Makefile:4-6`; tracked-file inventory |

Topology interpretation: this is a library-centric native codebase, with optimized/debug static archives and thin command-line front ends rather than a multi-process service topology. `E3 code-derived` — `src/Makefile:4-20`, `numutils/src/Makefile:8-86`. Whether downstream applications outside this checkout depend on the library is `E5 unknown`.

## 5. User-visible surfaces

| Surface | Trigger | Inputs | Outputs | Linked behavior ID(s) | Evidence grade | Source or pointer |
|---|---|---|---|---|---:|---|
| Sequence generators: `arange`, `linspace`, `logspace` | Invoke executable with numeric options | Start/end/step or count, read through `PARSE_CMD` | Numeric sequence to stdout | TBD | E3 code-derived | `numutils/src/arange.f90:1-29`; `numutils/src/linspace.f90:1-27`; `numutils/src/logspace.f90:1-26` |
| Matsubara frequency generator: `wmatsubara` | Invoke executable with frequency/type options | Inverse temperature, count, boson/fermion type | Matsubara frequencies to stdout | TBD | E3 code-derived | `numutils/src/wmatsubara.f90:1-35` |
| Random generator: `random` | Invoke executable with distribution options | Sample count, distribution, mean/variance/range parameters | Random values to stdout | TBD | E3 code-derived | `numutils/src/random.f90:1-45` |
| Numerical derivative: `deriv` | Pipe numeric columns to stdin | X/Y columns, derivative order/options | X and derivative values to stdout | TBD | E3 code-derived | `numutils/src/deriv.f90:1-65` |
| Fermi transform: `fermi` | Pipe numeric values to stdin | Values plus beta/mu options | Input and transformed values to stdout | TBD | E3 code-derived | `numutils/src/fermi.f90:1-46` |
| Spline interpolation: `spline` | Pipe X/Y points to stdin | Data columns, interpolation range/count/options | Interpolated values to stdout | TBD | E3 code-derived | `numutils/src/spline.f90:1-77` |
| Fourier/Green-function transform: `fftgf` | Pipe real/complex series to stdin | Transform type, sign, beta, input columns | Transformed frequency/time series to stdout | TBD | E3 code-derived | `numutils/src/fftgf.f90:1-86` |
| Expression evaluation: `func` | Pipe X values to stdin with expression option | Expression string `f(x)` and values | X and evaluated result to stdout | TBD | E3 code-derived | `numutils/src/func.f90:1-12,57-61,83-86` |
| Histogram: `histogram` | Pipe samples to stdin | Values, bins/range/options | Bin center and density/count values to stdout | TBD | E3 code-derived | `numutils/src/histogram.f90:1-74` |
| Kernel density: `kdensity` | Pipe samples to stdin | Values, evaluation grid, bandwidth/options | Density estimates to stdout | TBD | E3 code-derived | `numutils/src/kdensity.f90:1-68` |
| Numerical statistics: `numstat` | Pipe one or more numeric columns to stdin | Column selection and covariance options | Moments/statistics or covariance values to stdout | TBD | E3 code-derived | `numutils/src/numstat.f90:1-86` |
| Padé continuation: `pade` | Invoke with input/output/range options | Complex data file or stdin-derived arrays; continuation controls | Real-frequency complex data file/stdout | TBD | E3 code-derived | `numutils/src/pade.f90:1-67` |
| Complex-column expansion: `ffcmplx` | Invoke with input filename | Complex-valued data file | `.abs`, `.arg`, `.real`, `.imag` sibling files; removes prior outputs | TBD | E3 code-derived | `numutils/src/ffcmplx.f90:1-61` |
| 3D scalar plot: `splot` | Pipe scalar/complex grid values to stdin | Numeric values plus X/Y dimensions, column count, output filename, and grid/line options | Plot data plus executable Gnuplot shell script, or grid data only | TBD | E3 code-derived | `numutils/src/splot.f90:22-48,79-99,119-155`; `src/slplot_splot_3d.f90:17-68` |
| Vector-field plot: `vfplot` | Supply vector-field data filename | X/Y/U/V data and plot options | DISLIN plot output/window | TBD | E3 code-derived / E5 unknown | `numutils/src/vfplot.f90:1-8,35-42` |
| Library API | Compile/link a Fortran consumer using module interfaces | Typed scalar/array arguments and callbacks | Numerical return values, allocated arrays, files, and console messages depending on module | TBD | E3 code-derived | `src/SCIFOR.f90:1-17`; representative interfaces at `src/MATRIX.f90:9-45`, `src/INTEGRATE.f90:8-34`, `src/GREENFUNX.f90:80-91` |
| Console diagnostics | Calls to `msg`, `warning`, `error`/`abort` | Text and optional ANSI style | Standard output; `error` terminates via `STOP` | TBD | E3 code-derived | `src/COMVARS.f90:86-103,189-227,235-254` |

No linked `BEH-NNN` behavior artifacts exist yet, so behavior IDs are intentionally `TBD`. Observed command names and code paths are not treated as confirmed product requirements.

## 6. Data stores and file formats

| Store / format | Purpose | Read / write paths | Ownership / lifecycle | Evidence grade | Source or pointer |
|---|---|---|---|---:|---|
| Plain-text numeric streams | Primary CLI interchange for vectors, paired columns, and complex values | Standard input and standard output via list-directed/formatted Fortran I/O | Caller-owned pipeline data | E3 code-derived | Representative reads/writes: `numutils/src/deriv.f90:37-65`, `numutils/src/numstat.f90:40-86`, `numutils/src/fftgf.f90:49-86` |
| Plain-text numeric files | Library and utility input/output for arrays, columns, and complex data | Paths supplied to `IOTOOLS`, `IOFILE`, `pade`, and `ffcmplx` | Caller-owned; utilities may overwrite/create siblings | E3 code-derived | `src/IOTOOLS.f90:1-50,164-214`; `src/IOFILE.f90:141-150,246-263`; `numutils/src/ffcmplx.f90:29-61` |
| Gzip-compressed text (`*.gz`) | Compressed form of text data files | `IOFILE` can gzip files over a size threshold and gunzip an absent base file when a `.gz` sibling exists | Input is expanded in place; recompression is a separate `data_store` operation | E3 code-derived | `src/IOFILE.f90:174-203,219-268` |
| Gnuplot scripts and plot data | Plot configuration and numerical rendering input | Runtime data and `.gp` files; checked-in example `plot_prova.plot` | Runtime scripts/data are generated by `SLPLOT`; fixtures are repository-owned | E3 code-derived | `src/slplot_splot_3d.f90:17-68`; `numutils/src/splot.f90:119-155`; `numutils/test/plot_prova.plot:1-20` |
| Lattice/DOS text outputs | Reciprocal-lattice and density-of-states tabulations | `*.lattice` and default `DOSfree.lattice`, moved into `LATTICEinfo/` when storage is enabled | Library-generated local files/directories | E3 code-derived | `src/SQUARE_LATTICE.f90:131-160`; `src/TOOLS.f90:250-276` |
| Convergence/error traces | Optional convergence diagnostic output | Default `error.err`, caller-selected filenames, and `ERROR.README` on iteration exhaustion | Created/overwritten by convergence helpers | E3 code-derived | `src/tools_check_scalar.f90:4-31`; `src/tools_test_convergence.f90:1-13` |
| Build/version metadata include | Embeds a Git revision string into the library | `src/scifor_version.inc`, rewritten by `make version` | Generated from repository Git state but currently checked in | E3 code-derived | `src/Makefile:2,24-25`; `src/scifor_version.inc:1`; `src/COMVARS.f90:9,112-124` |
| Test/characterization-looking assets | Numeric inputs/outputs, derivative-looking output, histogram data, Gnuplot data/script, and a Grace project | `numutils/test/*.data`, `*.dat`, `*.deriv`, `*.plot`, `plot1`, `plot2`, `pdf.dat$` | Repository-owned fixtures; provenance and pass criteria unknown | E3 code-derived / E4 inferred | `numutils/test/xy2.data:1-10`; `numutils/test/xy2.deriv:1-10`; `numutils/test/hist.dat:1-10`; `numutils/test/plot_prova.plot:1-20`; `numutils/test/pdf.dat$:1-20` |
| Database/persistent service store | None found in this checkout | Not applicable | Not applicable | E3 code-derived | Whole-tree inventory and searches found no schema, database client, connection string, or service persistence implementation |

File-format caveat: list-directed Fortran text output does not by itself define stable delimiters, exponent spellings, locale behavior, precision, or NaN/Infinity spelling across compilers. Those compatibility details are `E5 unknown` until executable output is captured.

## 7. External interfaces and integrations

| Interface | Direction | Protocol / transport | Data contract | Failure behavior | Evidence grade | Source or pointer |
|---|---|---|---|---|---:|---|
| Shell command line | Inbound | Process arguments via Fortran runtime | `NAME=value` parsing plus help flags; per-command help text | Malformed values use list-directed reads; help paths `STOP`; malformed-input behavior is unverified | E3 code-derived / E5 unknown | `src/PARSECMD.f90:36-61,87-100,109-191`; representative registration at `numutils/src/random.f90:18-29` |
| Standard input/output | Inbound / outbound | Fortran units `5`, `6`, and `*` | Mostly whitespace-delimited numeric scalars/columns, sometimes complex values | EOF terminates reads; malformed-input behavior varies and is unverified | E3 code-derived / E5 unknown | `src/COMVARS.f90:53-55`; representative loops at `numutils/src/deriv.f90:37-65` and `numutils/src/fftgf.f90:49-86` |
| Filesystem | Inbound / outbound | Native Fortran file I/O | Caller paths, numeric text, generated scripts, diagnostic files | `inquire`, `iostat`, and `error` are used unevenly; some utilities delete/overwrite files | E3 code-derived | `src/IOFILE.f90:130-203,219-300`; `numutils/src/ffcmplx.f90:29-61` |
| Unix shell tools | Outbound | `call system(...)` | Shell command strings for `mkdir`, `mv`, `gzip`, `gunzip`, `chmod`, and file removal | Exit statuses are generally not checked | E3 code-derived | `src/IOFILE.f90:219-292`; `src/TOOLS.f90:250-276`; `src/slplot_splot_3d.f90:38-68`; `numutils/src/ffcmplx.f90:29-35` |
| BLAS/LAPACK or MKL linear algebra | Outbound native calls | Fortran ABI/static or dynamic libraries | Standard numerical routine signatures selected by link configuration | Library-specific status codes; wrapper handling varies | E3 code-derived / E5 unknown | `include/sfmake.inc:7-28`; representative calls at `src/MATRIX.f90:1283-1306` |
| Intel MKL DFT | Outbound native calls | `MKL_DFTI` Fortran module | Descriptor configuration and complex arrays | Checks descriptor status through `DftiErrorClass` and aborts on error | E3 code-derived | `src/FFTGF_MKL.f90:23-54,83-109` |
| FFTW3 | Outbound native calls | Legacy FFTW Fortran wrapper symbols | FFT plans and complex arrays | Error behavior not mapped; backend is not checked-in default | E3 code-derived / E5 unknown | `src/FFTGF_FFTW3.f90:83-95,128-142`; `src/fftw3.f:1-30` |
| `libmatheval` | Outbound native calls | C library symbols with underscore naming | Expression string handle and scalar evaluation | Null/parse/evaluation failure handling not evident in utility | E3 code-derived / E5 unknown | `numutils/src/func.f90:57-61,83-86`; `numutils/src/Makefile:44-46` |
| Gnuplot | Outbound process/file integration | Generated shell/`.gp` script and data files | Gnuplot commands, labels, ranges, palette, and terminal/output | Availability/exit status is not checked in mapped generation path | E3 code-derived / E5 unknown | `numutils/src/splot.f90:22-47,141-155`; `src/slplot_splot_3d.f90:17-68` |
| DISLIN/DLPLOT plotting API | Outbound native calls | External Fortran plotting module/library | Vector plot calls; exact contract/version not recovered | Module source and active link configuration are absent | E3 code-derived / E5 unknown | `numutils/src/vfplot.f90:1-6`; commented configuration at `include/sfmake.inc:53-61`; `bin/sciforvars.sh:54-63` |
| Git command | Outbound build-time process | `git rev-parse HEAD` | Full revision hash embedded in include/config | Build metadata can become stale if `version` is not run | E3 code-derived | `src/Makefile:2,24-25`; `include/sfmake.inc:31-33`; `src/scifor_version.inc:1` |
| Network/API integration | None found | Not applicable | Not applicable | Not applicable | E3 code-derived | Whole-tree inventory found no HTTP, socket, RPC, messaging, or remote-service client implementation |

## 8. Test and verification assets

| Asset | Type | Coverage | Reliability | Evidence grade | Source or pointer |
|---|---|---|---|---:|---|
| `numutils/test/` numeric files | Input/output-like fixtures | Constant vectors, X grids, X/Y grids, derivative-looking output, histogram/PDF data, and plot datasets | Data is readable, but expected-vs-input roles, generating version, precision, and pass criteria are undocumented | E3 code-derived / E4 inferred | `numutils/test/x.data:1-10`; `numutils/test/xy2.data:1-10`; `numutils/test/xy2.deriv:1-10`; `numutils/test/hist.dat:1-10`; `numutils/test/pdf.dat:1-10` |
| `plot_prova.plot` and `pdf.dat$` | Manual visualization artifacts | Gnuplot script and Grace project respectively | Can aid visual comparison, but no automated assertion or expected image is present | E3 code-derived | `numutils/test/plot_prova.plot:1-20`; `numutils/test/pdf.dat$:1-20` |
| Core numerical routine comments | Algorithm references and local contracts | QUADPACK, MINPACK, special functions, precision routines | Useful provenance, not executable verification | E3 code-derived | `src/integrate_d_quadpack.f90:1-16,193-217`; `src/minpack.f90:37-55`; `src/functions_special_funcs.f90:7-29` |
| Build dry runs | Environment/setup probe | Core archive and CLI build entrypoints | Verified current checkout cannot dry-run as a clean build; does not verify numeric behavior | E1 verified | `make -n -C src` fails at missing `options.inc`; CLI dry-run exposes unconfigured compiler/dependency paths |
| Shell syntax checks | Static syntax probe | `bin/setup_sf.sh`, `bin/sciforvars.sh` | Both parse as Bash; runtime prompts, mutation, path assumptions, and dependency behavior unverified | E1 verified | `bash -n` on both scripts on `2026-08-09`; source at `bin/setup_sf.sh:1-266`, `bin/sciforvars.sh:1-91` |
| Automated test runner/assertions | None found | No mapped unit, integration, regression, or CI suite | No automated pass/fail oracle available | E3 code-derived / E5 unknown | Whole-tree inventory; `numutils/test/` contains data/scripts but no Makefile or test program |
| Built executables/libraries | None present | No black-box execution coverage | Cannot establish `E1 verified` product behavior from this checkout | E1 verified | Empty `.bin/`, `lib/`, and source output directories on `2026-08-09` |
| External oracle | TBD | Potential upstream SciFortran checkout/release or known working installation | Identity, version, and availability not established | E5 unknown | `README.md:1` names an upstream URL but does not pin a release or oracle |

No fixture is promoted to a golden master in this map. The filenames suggest expected numerical outputs (`E4 inferred`), but only provenance or successful execution against a trusted legacy oracle can establish that role.

## 9. Modernization hotspots

| Hotspot | Evidence | Risk | Candidate first action | Evidence grade | Source or pointer |
|---|---|---|---|---:|---|
| Non-reproducible build bootstrap | `src/Makefile` includes local `options.inc`; tracked options live under `include/` and `library.conf` relies on a shell alias adding `-I$SFROOT/include`. The CLI Makefile instead depends on untracked local include files | Builds depend on shell aliases, checkout-local files, environment variables, and machine paths | Reconstruct and document a read-only-safe build recipe in the target workspace before behavior capture | E1 verified / E3 code-derived | `src/Makefile:1`; `etc/library.conf:1-9`; `numutils/src/Makefile:1-5`; Git status; `include/options.inc:1-16` |
| Broken interactive setup path | Compiler validation is logically always true; expected `sf/`/`local/` layout is not tracked; FFT link source/target names do not match repository files; final script sources absent `mylibvars.sh` | The supplied installer cannot be treated as a reproducible build oracle | Recover an actual successful setup transcript and required directory/dependency bundle | E3 code-derived / E5 unknown | `bin/setup_sf.sh:7-15,31-40,111-163,193-205`; tracked-file inventory |
| Missing `ZEROS` source/module | `SCIFOR` imports `ZEROS`, but no tracked matching source or build target exists | Facade compilation can fail or expose an incomplete public API | Recover the exact missing module/revision from provenance or a trusted installation | E3 code-derived / E5 unknown | `src/SCIFOR.f90:1-17`; `src/Makefile:4-6`; tracked-file inventory |
| FFT backend inconsistency | Checked-in `FFTGF.f90` includes MKL; FFTW3/NR implementations exist; setup attempts to link differently named, absent `FFT_*` files to misspelled `FFTGF.90` | Builds and numerical results may vary by undocumented local backend, and setup selection is currently broken | Identify the production backend and capture backend-specific fixtures/precision | E3 code-derived / E5 unknown | `src/FFTGF.f90:1-8`; `src/FFTGF_FFTW3.f90:1-20`; `src/FFTGF_NR.f90:1-35`; `bin/setup_sf.sh:193-202` |
| No executable oracle or automated assertions | No binaries/libraries and only unprovenanced data/plot assets are present | Numerical parity and edge-case behavior cannot be verified | Locate a known-working installation or build, then characterize CLI/library outputs without altering legacy source | E1 verified / E5 unknown | Empty output directories; `numutils/test/*`; Section 8 |
| Very large bundled routine collections | Two source files are about 25k lines each, plus large QUADPACK/MINPACK files and include-based composition | Review, provenance, defect isolation, and faithful translation are difficult | Establish ownership/license/provenance and isolate callable surfaces before deeper behavior cataloging | E3 code-derived | `src/CHRPACK.f90:1-115`; `src/functions_special_funcs.f90:1-38`; `src/integrate_d_quadpack.f90:1-16`; `src/minpack.f90:37-55` |
| Licensing/provenance exposure | `mkl_lapack.fi` carries Intel confidential/proprietary terms; other bundled routines carry varied copyright/LGPL notices; no repository-level license was found | Redistribution or translation scope may be legally constrained | Obtain legal/provenance review and identify which checked-in third-party files are authorized inputs | E3 code-derived / E5 unknown | `src/mkl_lapack.fi:1-22`; `src/functions_special_funcs.f90:7-29`; `src/minpack.f90:37-48`; repository inventory |
| Shell/file side effects embedded in numerical I/O | Library code invokes `gunzip`, `gzip`, `mkdir`, `mv`, and `chmod`; utilities remove and overwrite files | Behavior depends on host tools, path quoting, permissions, and process status handling | Catalog each side effect and characterize failure behavior with disposable fixtures | E3 code-derived | `src/IOFILE.f90:219-292`; `src/TOOLS.f90:250-276`; `src/slplot_splot_3d.f90:38-68`; `numutils/src/ffcmplx.f90:29-35` |
| Compiler-kind and numeric tolerance assumptions | Public signatures use numeric kind values (`8`; `ddp=16` is declared) and compiler-specific flags; exact compiler/version is unknown | Precision, formatting, overflow, and convergence may change across compilers | Pin the legacy compiler/library versions and record numeric tolerances before parity decisions | E3 code-derived / E5 unknown | `src/COMVARS.f90:13-39`; `include/options.inc:1-16` |
| Control-flow and legacy numerical complexity | Whole-source scan found `GOTO` in FFT, random, spline, QUADPACK, MINPACK, and special-function routines; implementation is heavily include-composed | Hidden control-flow and shared algorithm assumptions increase characterization effort | Select externally callable routines and trace them from public interface to included implementation | E3 code-derived | Representative sites: `src/FFTGF_NR.f90:154-180`; `src/random_routines.f90:48-69`; `src/integrate_d_quadpack.f90:1178-1203`; `src/minpack.f90:401-418` |
| Shared mutable process state | `COMMON_VARS` exports MPI/OpenMP-named globals; intrinsic RNG state is reseeded from `SYSTEM_CLOCK` | Reentrancy, determinism, and parallel behavior may depend on process/module state and invocation timing | Characterize repeatability and concurrent-call assumptions before declaring behavior contracts | E3 code-derived / E5 unknown | `src/COMVARS.f90:59-67`; `src/RANDOM.f90:197-208` |
| Utility build/surface drift | Default `all` omits `ffcmplx` and `vfplot`, though both sources and explicit targets exist | Actual shipped CLI surface cannot be inferred from source presence alone | Confirm which executables users actually install and invoke | E3 code-derived / E5 unknown | `numutils/src/Makefile:8,24-26,84-86` |
| Stale embedded version | Checked-in `scifor_version.inc` names revision `722cde3...`, while current HEAD is `5e4e6a3...` | Diagnostics can report a revision different from source actually built | Determine whether release builds always regenerate version metadata | E1 verified / E3 code-derived / E5 unknown | `src/scifor_version.inc:1`; `src/Makefile:2,24-25`; current Git HEAD |

Measured source-size facts above are repository inventory results, not quality judgments. Risk statements are mapping interpretations and do not choose a target design or migration sequence.

## 10. Open mapping questions

| Question | Why it matters | Evidence needed | Owner | Status |
|---|---|---|---|---|
| Which commit/tag/release is the production legacy baseline? | Current HEAD, embedded version, and README clone provenance do not identify one canonical release | Production binary/source hash, release record, or user decision | Product/legacy owner | E5 unknown |
| Which compiler and exact version produced trusted outputs? | Kind sizes, flags, I/O formatting, ABI, and numerical behavior can vary | Build log, environment capture, or working host | Legacy operator | E5 unknown |
| What exact setup/build command and directory layout are canonical? | Checked-in Makefiles depend on symlinks/environment not present in a clean checkout | Repeatable build transcript and required generated links/files | Legacy maintainer | E5 unknown |
| Where is the required `ZEROS` module? | The umbrella module imports it, but the tracked source/build list does not provide it | Source file or evidence the dependency should be absent from this baseline | Legacy maintainer | E5 unknown |
| Which FFT backend is production: MKL, FFTW3, or Numerical Recipes? | Backend affects dependencies, normalization, edge cases, and precision | Production config/build log plus parity fixtures | Legacy operator | E5 unknown |
| Which BLAS/LAPACK/MKL/FFTW/libmatheval/DISLIN versions are required? | Native ABI and numerical results depend on external versions | Lockfile-equivalent inventory, package records, or working host | Legacy operator | E5 unknown |
| Is checked-in third-party source legally reusable or redistributable? | Intel-confidential text and mixed routine licenses create a provenance blocker | License grants, source provenance, and legal decision | Legal/product owner | E5 unknown |
| Which of the 17 CLI sources are actually user-facing/supported? | Source presence and Make targets do not establish product scope | Usage telemetry, scripts, docs, support history, or user decision | Product owner | E5 unknown |
| Are `numutils/test/` outputs trusted golden masters, and how were they generated? | They cannot support parity until provenance and tolerances are known | Generator commands, expected-input/output pairing, compiler/library versions | Legacy maintainer | E4 inferred / E5 unknown |
| Is there a working executable/library oracle? | Without one, behavior can only be code-derived or documented | Accessible installation, archived binary, or reproducible build | Legacy operator | E5 unknown |
| What formatting and numerical tolerance are part of compatibility? | Text output and floating-point values are not fully specified by source names | Captured outputs and product decision on exact/relative/semantic comparison | Product owner | E5 unknown |
| Are MPI/OpenMP variables and flags operational requirements or abandoned scaffolding? | Parallel state could affect determinism and deployment assumptions | Working invocation, build flags, docs, or history | Legacy maintainer | E5 unknown |
| What external downstream programs link `libscifor.a`? | The repository does not reveal all public API consumers | Consumer inventory and link/build manifests | Product/legacy owner | E5 unknown |

**Tensions / conflicts**

- The user-supplied legacy path is `/Users/philipteitel/code/ADD-migrations/sci-fortran-legacy`, while the workflow profile contains the mismatched `../scifortran-legacy`; this run used the explicit user override. `E2 documented` — binding request; `[target] .cursor/workflow.config.yml:2`.
- The active workflow profile is structurally inconsistent around `compiler` (a scalar followed by nested `name`/`version`) and misspells `knownHazards` as `knonHazards`; source-stack resolution therefore relies on repository evidence and explicit user context rather than parsed values. `E3 code-derived` — `[target] .cursor/workflow.config.yml:8-17`.
- `src/Makefile` requires a local `options.inc`, but only `include/options.inc` is tracked; `etc/library.conf` works around this with a shell alias adding an include search path. The CLI Makefile instead relies on two untracked local configuration files. `E1 verified / E3 code-derived` — `src/Makefile:1`; `etc/library.conf:9`; `numutils/src/Makefile:1-5`; Git status.
- `setup_sf.sh` sources `bin/mylibvars.sh`, but the tracked environment script is `bin/sciforvars.sh`; no tracked `mylibvars.sh` exists. `E3 code-derived` — `bin/setup_sf.sh:204`; `bin/sciforvars.sh:1-50`; tracked-file inventory.
- `setup_sf.sh` tries to select `FFT_MKL.f90`, `FFT_FFTW3.f90`, or `FFT_NR.f90` and link to `FFTGF.90`; the tracked files are named `FFTGF_MKL.f90`, `FFTGF_FFTW3.f90`, `FFTGF_NR.f90`, and `FFTGF.f90`. `E3 code-derived` — `bin/setup_sf.sh:193-202`; tracked-file inventory.
- `SCIFOR` imports `ZEROS`, but no tracked source or default build target defining it was found. `E3 code-derived / E5 unknown` — `src/SCIFOR.f90:1-17`; `src/Makefile:4-6`.
- The checked-in FFT facade selects MKL, while FFTW3 and Numerical Recipes implementations are also present; the production choice is unresolved. `E3 code-derived / E5 unknown` — `src/FFTGF.f90:1-8`; `src/FFTGF_FFTW3.f90:1-20`; `src/FFTGF_NR.f90:1-35`.
- The default utility build omits `ffcmplx` and `vfplot`, even though source files and explicit targets exist. `E3 code-derived / E5 unknown` — `numutils/src/Makefile:8,24-26,84-86`.
- The checked-in embedded revision (`722cde3...`) differs from repository HEAD (`5e4e6a3...`). `E1 verified / E3 code-derived` — `src/scifor_version.inc:1`; current Git HEAD.
- Workflow hazards list COMMON blocks, EQUIVALENCE, implicit typing, and fixed-form I/O; whole-source scans found no `COMMON` or `EQUIVALENCE` statements and widespread `implicit none`, but did find fixed-form FFTW content and multiple `GOTO` paths. Treat configured hazards as hypotheses, not established facts. `E3 code-derived` — `[target] .cursor/workflow.config.yml:17-24`; representative `src/fftw3.f:1-30`, `src/FFTGF_NR.f90:154-180`.
- The profile labels the oracle tier `T3 documented-only`; checked-in test-like data exists but has no verified provenance, so it does not currently justify a stronger oracle tier. `E3 code-derived / E4 inferred` — `[target] .cursor/workflow.config.yml:31`; `numutils/test/xy2.data:1-10`, `numutils/test/xy2.deriv:1-10`.

## 11. Links

- Assessment: not created or linked by this phase.
- Dependency ledger: not created or linked by this phase.
- Translation gaps: not created or linked by this phase.
